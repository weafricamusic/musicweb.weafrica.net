import 'package:flutter/material.dart';

/// Stub: Battle lobby screen - displayed when waiting for a battle to start.
class BattleLobbyScreen extends StatelessWidget {
  const BattleLobbyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Battle Lobby')),
      body: const Center(
        child: Text('Battle lobby coming soon'),
      ),
    );
  }
}